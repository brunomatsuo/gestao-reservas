services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=reservadatabase'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=user'
    ports:
      - '5436:5432'
